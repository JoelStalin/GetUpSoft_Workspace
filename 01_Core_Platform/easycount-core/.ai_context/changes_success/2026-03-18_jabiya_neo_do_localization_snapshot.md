# Successful Change - 2026-03-18

- Connected to SSH environment `jabiya`.
- Copied the active `neo_do_localization` tree from `/opt/odoo-docker/addons/neo_do_localization`.
- Imported it under `integration/odoo/neo_do_localization`.
- Removed nested VCS/cache artifacts from the copied tree.
- Added project-specific mapping and metadata so the import fits the `odoo_integration` direction of this repository.
