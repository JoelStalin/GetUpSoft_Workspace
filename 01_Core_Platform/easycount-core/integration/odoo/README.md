# Odoo Integration Assets

Este directorio agrupa componentes y referencias para la integracion opcional con Odoo descrita en `docs/guide/16-arquitectura-eks.md`.

Contenido actual:

- `odoo15_getupsoft_do_localization`: snapshot original obtenido por SSH desde `jabiya`, mantenido como baseline Odoo 15.
- `odoo19_getupsoft_do_localization`: refactor local del mismo paquete, elevado a `Odoo 19.0`, con nombres tecnicos `getupsoft_*`, vistas migradas y documentacion alineada con `Tenant` e `Invoice`.
