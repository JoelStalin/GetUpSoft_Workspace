# Integration Assets

Este directorio contiene integraciones externas y vendor trees que no forman parte directa del backend FastAPI principal.

Contenido actual:

- `odoo/odoo15_getupsoft_do_localization`: snapshot bruto del arbol Odoo 15 traido desde el ambiente `jabiya`.
- `odoo/odoo19_getupsoft_do_localization`: variante refactorizada localmente, con branding GetUpSoft y mapeo adaptado a `dgii_encf`.
