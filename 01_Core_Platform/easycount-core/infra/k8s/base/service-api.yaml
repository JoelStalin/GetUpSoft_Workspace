apiVersion: v1
kind: Service
metadata:
  name: getupsoft-api
  labels:
    app.kubernetes.io/name: getupsoft-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: getupsoft-api
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
