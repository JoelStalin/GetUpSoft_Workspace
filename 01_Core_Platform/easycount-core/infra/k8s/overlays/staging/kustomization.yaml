apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
nameSuffix: -stg
configMapGenerator:
  - name: api-settings
    behavior: merge
    literals:
      - LOG_LEVEL=debug
      - FRONTEND_ADMIN_URL=https://admin.stg.getupsoft.do
      - FRONTEND_CLIENT_URL=https://app.stg.getupsoft.do
patches:
  - path: patch-deployment.yaml
  - path: patch-ingress.yaml
