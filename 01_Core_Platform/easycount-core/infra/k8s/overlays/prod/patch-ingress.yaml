apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: getupsoft-api
spec:
  rules:
    - host: api.getupsoft.do
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: getupsoft-api
                port:
                  name: http
  tls:
    - hosts:
        - api.getupsoft.do
      secretName: api-tls
