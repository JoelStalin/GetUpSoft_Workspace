apiVersion: v1
kind: Service
metadata:
  name: getupsoft-site
  namespace: getupsoft
  labels:
    app.kubernetes.io/name: getupsoft-site
    app.kubernetes.io/part-of: getupsoft-workspace
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: getupsoft-site
  ports:
  - name: http
    port: 5173
    targetPort: http
    protocol: TCP
