pywin32>=306
Pillow>=10.2.0
PyPDF2>=3.0.0
requests>=2.31.0

# Notas:
# - 'tkinter' (tk) y 'ssl' vienen con Python en Windows; no se instalan por pip.
# - Dependencias transitivas de 'requests' (certifi, urllib3, idna, charset-normalizer)
#   no deben fijarse aquí a menos que sea estrictamente necesario.
