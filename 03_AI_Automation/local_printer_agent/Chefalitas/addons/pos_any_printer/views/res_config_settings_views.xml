<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.account</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="account.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='default_taxes']" position="after">
                <setting id="any_printer_settings">
                    <div class="content-group">
                        <field name="any_printer_ip" placeholder="Any Receipt Printer IP Address" />
                    </div>
                    <div role="alert" class="alert alert-warning"
                    >
                        The Any receipt printer will be used instead of the receipt printer
                        connected to
                        the IoT Box.
                    </div>
                </setting>

            </xpath>
        </field>
    </record>

</odoo>