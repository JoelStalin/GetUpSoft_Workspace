<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>

    <!-- POS Config (per POS) -->
    <record id="pos_config_view_form_inherit_local_printer" model="ir.ui.view">
      <field name="name">pos.config.form.inherit.local.printer</field>
      <field name="model">pos.config</field>
      <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
      <field name="arch" type="xml">
        <!-- Use a very stable anchor: every pos.config form has a sheet -->
        <xpath expr="//sheet" position="inside">
          <group string="Impresión Local (Agente Windows)">
            <field name="enable_local_printing"/>
            <field name="local_printer_agent_url" placeholder="http://127.0.0.1:9060"/>
            <field name="local_printer_cashier_name" string="Impresora (Caja)" placeholder="EPSON TM-T20II"/>
            <field name="local_printer_kitchen_name" string="Impresora (Cocina)" placeholder="EPSON TM-U220"/>
            <field name="local_printer_print_as_image"/>
            <field name="local_printer_image_width" placeholder="576"/>
            <span class="o_form_label">Nota:</span>
            <div class="text-muted">
              Configure el agente en la PC y use el nombre EXACTO de la impresora (según /printers del agente).
            </div>
          </group>
        </xpath>
      </field>
    </record>

  </data>
</odoo>
