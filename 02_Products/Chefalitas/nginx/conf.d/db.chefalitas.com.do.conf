# Fase 1: proxy HTTP (Certbot + pgAdmin sin SSL)
# server {
#     listen 80;
#     server_name db.chefalitas.com.do;

#     # Challenge de Let's Encrypt (permite renovar certificados con HTTP-01)
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }

#     # Mientras no exista el certificado, servimos pgAdmin por HTTP
#     location / {
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_pass http://pgadmin:80;

#         proxy_read_timeout 90s;
#     }
# }

# Fase 2: activar redirección y proxy sobre HTTPS (descomentar cuando haya certificado)
server {
    listen 80;
    server_name db.chefalitas.com.do;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name db.chefalitas.com.do;

    ssl_certificate /etc/letsencrypt/live/chefalitas.com.do/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chefalitas.com.do/privkey.pem;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_pass http://pgadmin:80;   # 👈 OJO AQUÍ
        proxy_read_timeout 90s;
    }
}
