# Next Steps

1. Cargar secretos reales DGII en `TEST` y ejecutar la misma matriz contra servicios oficiales.
2. Configurar Odoo 19 JSON-2 real y validar `SYNCED_TO_ODOO`.
3. Ejecutar la migración en una base staging y consolidar duplicados antes de hacer cumplir restricciones únicas donde aplique.
4. Conectar el admin portal al stream SSE real o mantener polling controlado según política operativa.
5. Ejecutar browser automation solo en modo `evidence-only` o `assistive` contra un flujo autorizado.
