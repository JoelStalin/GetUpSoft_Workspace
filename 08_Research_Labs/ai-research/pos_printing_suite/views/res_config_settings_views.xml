<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- POS Printing Suite: configure per POS in Point of Sale → Configuration → each POS form (Printing mode, Local Agent device, printer names). -->
</odoo>
