# Customer Data Context

This directory (`/context/customers/`) is reserved for future AI systems to store anonymized profiles, VIP customer markers, and general audience insights that shouldn't be hardcoded into the business logic.

*Note: Production Personally Identifiable Information (PII) must be stored in a compliant database, local SQLite/JSON restricted area, or CRM.*
