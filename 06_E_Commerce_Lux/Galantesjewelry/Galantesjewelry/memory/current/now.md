# Current State - Galantes Jewelry Production Recovery

## Project Phase
Production environment recovered after the failed April 28, 2026 deploy.

## Current Task
Production is stable again. The shop regressions, Google OAuth / Calendar recovery path, the Odoo category fallback for uncategorized products, and the invalid `shop_hero_image_url` settings lookup were fixed and revalidated. On April 30 the Google login endpoint and calendar availability checks were revalidated again after restoring `data/integrations.json`, and the production Cloudflare tunnel was reconnected on the live VM. The checkout now exposes insured shipping carrier selection, recalculates shipping server-side, and records a shipping service line in Odoo so Stripe and Odoo totals match. No rollback was executed after the user corrected the request. Recent performance work reduced build time by removing Turbopack workspace-root inference and caching Odoo settings in the root layout.
The restored operational CMS data is now persisted back into `galante.cms.settings` with the logo, hero image, contact address, email, phone, Instagram, Facebook, WhatsApp, and navigation JSON so Odoo remains the durable source of truth if the local cache is ever rebuilt.
On May 5, 2026, `shop.galantesjewelry.com` was re-routed from the legacy Odoo storefront profile to the Next.js storefront profile, so `/shop/watche` and the product image proxy now respond on both the shop and apex domains.
On May 5, 2026, I also fixed the checkout tax flow so tax is based on the shipping or pickup destination rather than the payment method. Stripe now charges the final Odoo order total, and the cart/checkout thumbnails fall back to `/api/products/image?id=...` when a cart item is missing `image_url` data.
The account layout spacing has now been widened locally so the fixed navbar no longer overlaps the `/account` content in source, but the live production smoke still reflects the previously deployed build until that code is redeployed.
The GCP repo sync path now preserves the live `data/` tree before `git reset --hard` and restores it afterward, so code deploys no longer risk wiping CMS or integration state stored in the repo.
I also created a rollback backup on the production VM at `/home/yoeli/galantesjewelry/backups/predeploy/20260501_023923`; it includes `galantes_db.sql`, `postgres-data.tgz`, `odoo-data.tgz`, `app-data.tgz`, and runtime logs/inspects for the live stack.
The runtime-only release package was then deployed to `/home/yoeli/galantesjewelry` on the GCP VM, the `galantes_web` container was rebuilt with `.env.prod`, and the public health endpoint returned `ok` after cutover.
On May 6, 2026, the custom Odoo gallery image field `galantes.product.gallery.image` was promoted from a `1024x1024` cap to `4096x4096` so high-resolution phone photos are no longer downscaled that aggressively during product-image upload. The updated addon files were synced directly to the GCP VM, `galantes_odoo` was restarted, `/web/login` still returned HTTP 200 publicly, and the service came back healthy.
On May 8, 2026, the production catalog was intentionally reset for a clean inventory replacement. A full production database dump was created at `/home/yoeli/deploy-backups/inventory-reset-20260508_135719.galantes_prod.dump`, all 35 non-service/non-placeholder product templates were removed through the Odoo ORM with historical-reference preservation intact, service templates were forced off the storefront, and `galantes_web` was restarted so the public `/shop` page stopped serving cached fallback catalog entries.
Later on May 8, 2026, I repaired the production Odoo accounting baseline so paid-checkout finalization can be validated for real. The custom addon import tree was missing `sale_order`, so `action_galantes_finalize_paid_checkout` was never registered; I added `from . import sale_order` in `odoo/addons/galantes_jewelry/models/__init__.py`, synced it to the VM, and restarted only `galantes_odoo`. I then found the production company had no chart of accounts, journals, or payment method lines, installed the US accounting localization directly on `galantes_prod`, and loaded the `us` chart template for `My Company`, which created 106 accounts and 7 journals including Sales and Bank.
With accounting repaired, I created a temporary production QA flow around an out-of-stock test product, a real Odoo sale order, and a real Stripe test-mode PaymentIntent. Calling `sale.order.action_galantes_finalize_paid_checkout` on order `S00010` created exactly one posted invoice (`INV/2026/00001`), registered one inbound payment in the Bank journal, and remained idempotent on a second call with the same Stripe payload. I then executed a full Stripe refund (`re_3TUpEb9lJ2VzyN5R1HEgh1o7`) for the captured PaymentIntent, posted the Odoo credit note (`RINV/2026/00001`), registered the outbound refund payment, and confirmed Stripe rejects a second refund attempt with `charge_already_refunded`. The helper also correctly raises a `UserError` when the Stripe amount does not match the Odoo invoice total.
The temporary QA product was archived again after the validation so the storefront catalog stays effectively clean. The remaining functional gap is the public Next.js checkout -> Odoo creation path, which still needs a dedicated production pass because this validation used the controlled manual Odoo + Stripe path after the live checkout route had already been observed failing earlier in order creation.
The `/account/addresses` auth-guard follow-up is now complete, matching the redirect behavior used by the other account pages.
Customer login links now preserve the current URL through `returnTo`, so clicking login from the cart or another page brings the user back to the same location after authentication.
Customer sessions are now durable until logout as well, so the authenticated customer sections stay available across navigation without expiring on the short 30-day window.
The login-return flow was then verified with a real Selenium run against a local Next.js server: cart -> login -> cart worked, logout returned home, and `/account/orders` redirected back to login after logout.
I then tightened the customer auth surface: the shared account layout now redirects unauthenticated visitors centrally, and the login `returnTo` sanitizer rejects `/auth` routes so users cannot bounce through logout or other auth pages after signing in. That follow-up was validated again with targeted Vitest, lint, and a fresh Selenium run against `http://127.0.0.1:3001`; the browser logs showed only the expected development/Odoo fallback noise and the flow still passed end to end.
Next 16 then blocked the production build because both `middleware.ts` and `proxy.ts` were present, so I migrated the request-header logic into `proxy.ts`, removed `middleware.ts`, and hardened the GCP deploy scripts to use the real VM path and `sudo docker compose` against the actual compose services. After that the production stack rebuilt cleanly and the live Selenium QA against `https://galantesjewelry.com` confirmed the customer invoices page loads without redirecting to login.
On May 2, 2026, `https://odoo.galantesjewelry.com/` was found routing to Next.js instead of Odoo because the live nginx base config was not loading the mounted `conf.d-active` host rules. I updated `infra/nginx/nginx.conf` to define `$connection_upgrade`, removed the conflicting base `default_server`, included `/etc/nginx/conf.d/*.conf`, recreated only `galantes_nginx` so Docker remounted the corrected file, and verified `/web/login` returns the Odoo login form publicly.
Later on May 2, 2026, the production Odoo `admin` user password was rotated to the user-provided value via Odoo ORM (`res.users.write`) on `galantes_prod`; the secret was not written to repo files. Authentication was verified through `/web/session/authenticate` and returned `AUTH_OK`.
Also on May 2, 2026, I completed a production web hardening pass using the new agency-agents security/performance/testing guidance while preserving the repository Selenium Profile 9 rules. `/api/products/image` now rejects non-numeric IDs before any Odoo or cache access, guards cache paths with `path.relative`, and returns 404 instead of defaulting unknown valid product IDs to product 1. Admin/customer auth routes now have in-memory per-IP+identifier rate limits, admin credential comparison uses `crypto.timingSafeEqual`, Next security headers are active, and image remote patterns are restricted to Odoo `/web/image/**`. A selective web-only deploy rebuilt `galantes_web`; public checks confirmed `/api/health` 200 with new security headers, traversal probe 400, Odoo `/web/login` 200, and Docker services healthy. Selenium Profile 9 passed production product catalog and account navbar checks, including `/account/orders`, `/account/invoices`, and `/account/settings`. Performance spot checks after deploy: home 200 TTFB 0.215s total 0.236s; shop 200 TTFB 0.255s total 0.268s. Residual npm audit risk is limited to two moderate findings from PostCSS bundled inside Next 16.2.3; `npm audit fix --force` proposes a breaking downgrade to Next 9.3.3 and was not applied.
I then closed additional backlog hygiene: the admin appointments panel and `/api/admin/appointments` already provide Odoo sync status observability, filter support, Odoo partner/appointment IDs, and error visibility, so I added backend route coverage for valid/invalid `odooSyncStatus` filters and unauthorized access. The existing predeploy backup script already rotates old backups via `KEEP_BACKUPS`; a temporary local rotation test kept only the newest three directories. `.gitattributes` was added to normalize LF across the repo while preserving CRLF for Windows command scripts and binary asset handling.
On May 3, 2026 UTC, I improved the checkout fulfillment flow and deployed it to production. Signed-in checkout now preloads safe customer profile data from `GET /api/account/profile`, autocompletes name/email/phone/default address, shows saved Odoo address cards from `/account/addresses`, and keeps a manual address fallback. Fulfillment is now an explicit choice between `Ship insured` and `Boutique pickup`; pickup skips shipping address fields, sets shipping to free, and is accepted server-side by `/api/checkout/stripe` without address fields while delivery still requires a valid address and insured shipping rate. The checkout summary now avoids `next/image` for `/api/products/image?id=...` thumbnails because Next 16 localPatterns intentionally block broad local query-string optimization. I added backend unit coverage for pickup without address and Playwright coverage for account prefill, saved address selection, shipping rates, and pickup. The selected web files were deployed to the GCP VM after backup `/home/yoeli/deploy-backups/checkout-fulfillment-20260503-005404.tgz`; `galantes_web` rebuilt and restarted healthy. Production Playwright verification against `https://galantesjewelry.com` passed with API interception so no real payment/order was created.
Later in the same shipping pass, I wired a live FedEx rate client into `lib/shipping/engine.ts` so checkout can now present FedEx alongside the existing USPS, UPS, and pickup options. The FedEx integration uses OAuth client credentials, caches tokens, normalizes live rate quotes, and falls back to the existing estimated FedEx option when credentials are missing or the API is unavailable. The repo now documents the FedEx environment variables in `.env.example`, and unit tests cover both the live FedEx selection path and the fallback path.
I then ran a full Stripe test purchase validation. `app/api/checkout/stripe/route.ts` now creates PaymentIntents with `automatic_payment_methods.allow_redirects='never'` so the checkout can confirm a card payment without requiring a redirect `return_url`. A dedicated live integration test exercised the checkout POST route, confirmed the Stripe test PaymentIntent, and resolved the checkout status path successfully with Odoo mocked. The test passed with the provided Stripe test keys and FedEx sandbox credentials.
To make the shipping calculator easier to demo in the customer portal, I added a public ready-to-ship storefront item called `Shipping Calculation Demo Pendant` in `odoo/addons/galantes_jewelry/data/product_data.xml` and mirrored it in `lib/odoo/client.ts` fallback catalog data. Customers can add it to the cart and see live shipping totals in checkout without using an internal shipping service product.
I also added a shipping confirmation callout to `app/shop/[slug]/page.tsx` so the demo product page explicitly tells customers to add the item to cart and confirm FedEx, UPS, USPS, and boutique pickup rates at checkout. That callout is covered by `tests/unit/app/shop/product-page.test.tsx` and the repo still passes TypeScript, lint, and the production build after the change.
Follow-up checkout hardening was completed immediately after: choosing `Use a new address` now clears the previously selected saved-address fields, `/api/checkout/stripe` rejects pickup rates unless `deliveryMethod` is explicitly `pickup`, and `GET /api/account/profile` now has unit coverage for both authenticated and unauthenticated responses. The follow-up was validated with focused Vitest, TypeScript, lint, local Playwright, Next build, a second production backup `/home/yoeli/deploy-backups/checkout-edge-hardening-20260503-014217.tgz`, a web-only deploy, public health check, and production Playwright with API interception.
On May 6, 2026, I consolidated the production QA entrypoint into `tests/e2e/production_suite.py`. The suite now runs safe storefront/Odoo/appointments-preflight checks by default, keeps authenticated account smoke and real checkout/payment flows behind explicit flags, writes a single artifact summary, and routes older Selenium entrypoints back through the shared cloned-profile runtime so production verification stays operationally consistent.

## Next Actions
- Monitor live appointment submissions now that Google OAuth owner tokens and Calendar routing are restored.
- Commit the local recovery hardening and shop verification changes if they should become source-of-truth in git history.
- Monitor the new `Other` category fallback in Odoo now that the controller change is live.
- Keep the shipping selector flow under watch; the checkout now depends on the secure shipping service product and the shipping-rate API fallback.
- Keep `/account` redirects and Odoo settings reads on watch; the blank-page and invalid-field paths were just corrected.
- Account layout spacing fix was redeployed on May 2, 2026; live Selenium Profile 9 account-navbar smoke now passes.
- The `/account/addresses` auth guard fix is complete and validated.
- Customer login links now preserve `returnTo` for the current page, including the cart.
- Customer sessions now share the long-lived durability model used by Google sign-in until explicit logout.
- Real Selenium QA confirmed the cart login returnTo flow and logout blocking behavior.
- The shared account layout now owns the unauthenticated redirect, and auth-page `returnTo` values are sanitized before login completes.
- The latest Selenium pass against `http://127.0.0.1:3001` confirmed the hardened flow still works end to end.
- The production build now uses `proxy.ts` only; the Next 16 middleware/proxy conflict is gone.
- Production stack restart completed on the GCP VM, and live Selenium confirmed `/account/invoices` loads from a valid customer session without bouncing to `/auth/login`.
- Odoo backend public routing is restored: `https://odoo.galantesjewelry.com/web/login` now returns Odoo `200 OK`, nginx is healthy, and Selenium smoke `tests/e2e/odoo_public_login_smoke.py` passed.
- Odoo production `admin` password was rotated on May 2, 2026 and verified with JSON-RPC authentication; do not commit or document the plaintext secret.
- Keep the rollback path documented, but do not execute it unless explicitly requested again.
- Keep an eye on Odoo latency; the layout now times out quickly and falls back to local CMS data if Odoo is slow.
- Keep the shared skill docs under watch; the broken relative links in the skill tree were repaired and revalidated.
- Odoo JSON-2 404 responses now fail fast without retries, and the regression test suite passed for the client hardening.
- Appointment flow now emits structured Odoo sync diagnostics for success, skip, and failure paths, with a focused unit test in place.
- GCP production sync now snapshots `data/` around the repo update, which protects operational JSON state during the next handoff.
- Rollback backup is available on the VM at `/home/yoeli/galantesjewelry/backups/predeploy/20260501_023923`.
- Runtime-only production cutover completed on the live VM; `galantes_web` is healthy and serving the deployed build.
- Production web hardening is live: security headers are present, product image ID traversal returns 400, auth rate limits are covered by unit tests, and the account navbar smoke passed against production.
- Admin Odoo sync observability is covered in UI and backend tests; F-4 and O-4 are complete in the backlog.
- Backup rotation via `KEEP_BACKUPS` is verified; O-1 is complete in the backlog.
- `.gitattributes` LF normalization is now present; T-3 is complete in the backlog.
- Checkout fulfillment now supports signed-in autocompletion, saved address choice, and boutique pickup without requiring a shipping address. Production Playwright validation passed without creating a payment/order.
- Shipping selection now includes a live FedEx option alongside USPS, UPS, and pickup, with safe fallback behavior when FedEx credentials are absent or the rate API is down.
- The public demo product page is live in production at `/shop/shipping-calculation-demo-pendant`, and the live shipping-rate API returns pickup, USPS, UPS, and FedEx options for the demo item's destination.
- The live cart summary API for the demo product's $1250 subtotal returns USPS Priority Mail Express as the default shipping choice, $53.75 shipping, 7% Florida tax, and a $1395.01 total.
- A separate production checkout test product, `Checkout Test Pendant` with SKU `GJ-TEST-001`, was created directly in the live Odoo database without rebuilding the web app, and the storefront page `/shop/checkout-test-pendant` now responds with HTTP 200.
- After that, I reran the live Stripe purchase test against the production checkout using the test product, FedEx sandbox rates, and the provided Stripe test card; the end-to-end purchase flow passed.
- Production QA now has a single documented entrypoint: `npm run test:e2e:production` for safe smoke, or `npm run test:e2e:production:full` when live-mutation coverage is explicitly desired.
- Odoo gallery uploads now accept up to `4096x4096` in the custom `galantes.product.gallery.image` field; the next live check should be a real phone upload against a product record to confirm the previous failure mode is gone.
- Production storefront inventory was reset on May 8, 2026; only two internal service templates and the archival placeholder remain in Odoo, and the public shop now renders the empty-state UI.

## Active Contracts
- Cloudflare tunnel: Live and serving `galantesjewelry.com`, `shop.galantesjewelry.com`, and `odoo.galantesjewelry.com`
- Shop storefront host: `shop.galantesjewelry.com` now routes to the Next.js storefront, and `/shop/watche` plus `/api/products/image?id=31` return 200 on both the shop and apex hosts
- Web app: Healthy after rebuild from current VM source
- Odoo: Healthy after DB credential recovery
- CMS snapshot: Restored in both `data/cms.json` and `galante_cms_settings`
- Integrations snapshot: Restored, including the production Google OAuth config and calendar settings
- Shop controls: Sorting, category filters, PDP image gallery, and console health verified on production on April 28, 2026
- Selenium runtime: Uses the cloned local Chrome profile with extensions disabled to avoid third-party DOM injection during production verification
- Google OAuth admin callback: fixed to use the live request-derived redirect URI instead of a percent-encoded cookie value
- Production appointments: `googleCalendarId` restored to `ceo@galantesjewelry.com`
- Production Calendar auth: service account is preferred for Calendar operations; owner OAuth remains available for Gmail API mail delivery
- Odoo categories: uncategorized products now map to `Other` via `/api/products?category=Other` and `/api/categories`
- Odoo CMS settings: the client no longer requests the missing `shop_hero_image_url` field from `galante.cms.settings`
- Shipping checkout: carrier selection is available in `/checkout`, shipping is recomputed on the server, and Odoo now gets a dedicated secure shipping service line for totals parity
- Odoo admin host: `odoo.galantesjewelry.com` must route through nginx `conf.d-active` to `odoo:8069`; the base nginx config must include `/etc/nginx/conf.d/*.conf`

## Active Blockers
- The manual Odoo + Stripe paid-order/facture/refund path is now validated, but the public Next.js checkout -> Odoo order-creation path still needs a dedicated production fix/verification pass. Earlier live checkout attempts were failing before order creation, so the storefront checkout should not be treated as fully signed off yet.
- Remaining work beyond that is staging setup, external monitoring setup, appointment/Odoo live credential verification, product-image serialization follow-through, and git hygiene. Residual dependency audit item remains the Next-bundled PostCSS moderate advisory until a compatible Next release is available.

## Last Updated
2026-05-08 20:10 UTC
